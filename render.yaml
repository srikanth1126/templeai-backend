services:
  - type: web
    name: templeai-backend
    env: python
    buildCommand: pip install -r requirements.txt && python train_model.py
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0